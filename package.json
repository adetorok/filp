{
  "name": "homeflip",
  "version": "1.0.0",
  "description": "House flipping and rental management application",
  "private": true,
  "scripts": {
    "build": "cd client && npm ci --legacy-peer-deps && npm run build",
    "start": "cd client && npm start",
    "dev": "cd client && npm start",
    "install-client": "cd client && npm ci --legacy-peer-deps"
  },
  "dependencies": {
    "@prisma/client": "5.19.1",
    "@sentry/node": "^10.19.0",
    "axios": "^1.12.2",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.1.0",
    "express-validator": "^7.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "prisma": "5.19.1"
  },
  "engines": {
    "node": "20.x",
    "npm": ">=8.0.0"
  }
}
